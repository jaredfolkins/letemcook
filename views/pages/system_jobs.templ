package pages

import (
    "github.com/jaredfolkins/letemcook/models"
    "github.com/jaredfolkins/letemcook/views/layout"
)

templ SystemJobs(v models.SystemView) {
    <div class="mx-12 my-4">
        <h1 class="text-2xl font-bold">System Jobs</h1>
        <p>Work in progress.</p>
    </div>
}

templ SystemJobsIndex(v models.SystemView, cmp templ.Component) {
    @layout.Base(v.BaseView) {
        @cmp
    }
}
