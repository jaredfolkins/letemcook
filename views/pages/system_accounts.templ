package pages

import (
    "github.com/jaredfolkins/letemcook/models"
    "github.com/jaredfolkins/letemcook/views/layout"
)

templ SystemAccounts(v models.SystemView) {
    <div class="mx-12 my-4">
        <h1 class="text-2xl font-bold">System Accounts</h1>
        <p>Work in progress.</p>
    </div>
}

templ SystemAccountsIndex(v models.SystemView, cmp templ.Component) {
    @layout.Base(v.BaseView) {
        @cmp
    }
}
